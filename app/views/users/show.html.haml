- if @user == current_user
  %b Public profile
%h1
  = @user
  - if @user == current_user
    (You!)
    = link_to('Set display name', my_account_path) if @user.display_name.empty?
%h2
  Resources
%ul
  - @user.created_resources.each do |resource|
    %li
      = link_to resource.name, resource.url
      (#{link_to resource.topic, resource.topic})